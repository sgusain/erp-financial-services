apiVersion: v1
kind: Service
metadata:
  name: config-server
  namespace: erp-financial-services
  labels:
    app: config-server
    version: v1
    component: discovery
spec:
  type: ClusterIP
  selector:
    app: config-server
  ports:
    - name: http
      port: 8888
      targetPort: 8888
