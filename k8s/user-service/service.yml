apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: erp-financial-services
  labels:
    app: user-service
    version: v1
    component: business
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
    - name: http
      port: 8081
      targetPort: 8081
