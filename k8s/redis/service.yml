apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: erp-financial-services
  labels:
    app: redis
    version: v1
    component: infra
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
