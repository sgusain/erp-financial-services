apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: erp-financial-services
  labels:
    app: notification-service
    version: v1
    component: business
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - name: http
      port: 8085
      targetPort: 8085
